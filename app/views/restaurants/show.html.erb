<h2><%= @restaurant.name %></h2>
<p><%= @restaurant.address %></p>
<p><%= @restaurant.phone_number %></p>
<p><%= @restaurant.category%></p>
<p><strong>Reviews</strong></p>
<ul class="list-group">
  <% @restaurant.reviews.each do |review| %>
    <li class="list-group-item"><%= review.content %></li>
    <%# <%= link_to "Delete", review_path(review), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
  <% end %>
  <%= link_to 'Add a new review', new_restaurant_review_path(@restaurant) %>
</ul>
<br>

<%= link_to 'Retour à la liste', restaurants_path, class: "btn btn-primary" %>
<%# <%= link_to 'Edit a restaurant', edit_restaurant_path(@restaurant), class: "btn btn-primary" %>
<%# <%= link_to 'Delete', restaurant_path(@restaurant), data: {turbo_method: :delete, turbo_confirm: "Are you Sure?"}, class: "btn btn-primary" %>
